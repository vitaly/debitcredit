---
en:
  activerecord:
    errors:
      models:
        debitcredit/entry:
          unbalanced: item amounts must balance to 0
        debitcredit/account:
          overdraft: overdraft is not allowed

